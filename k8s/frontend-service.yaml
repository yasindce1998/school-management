apiVersion: v1
kind: Service
metadata:
  name: school-management-frontend
  labels:
    app: school-management-frontend
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: school-management-frontend
  type: ClusterIP
